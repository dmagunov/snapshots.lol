import styled from "styled-components";

import PixelButton from "components/PixelButton/PixelButton";

export const PixelButtonStyled = styled(PixelButton)`
  margin-top: 3rem;
`;

export const CloseButton = styled.button`
  margin-top: 2rem;
  background: none;
  border: none;
  cursor: pointer;
  font-size: 1.5rem;
`;
