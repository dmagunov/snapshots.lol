import styled from "styled-components";

export const CopiedMessage = styled.div`
  position: absolute;
  top: -1rem;
  left: 4rem;
  z-index: 4;
  color: #fff;
  padding: 1rem;
  white-space: nowrap;
`;
